<div class="section">
	<div class="container-fluid">
		<h2>Selección de Cursos - Nivel <%=get_course_level_name(current_user.course_level_id)%></h2>
		<div class="row">
			<div class="col-xs-12 col-sm-offset-4 col-sm-8">
				<div class="col-xs-4 theader first-month">
					<%=get_month_name(@selected_month.to_i)%>
				</div>
				<div class="col-xs-4 theader second-month">
					<%=get_month_name(@selected_month.to_i+1)%>
				</div>
				<div class="col-xs-4 theader third-month">
					<%=get_month_name(@selected_month.to_i+2)%>
				</div>
			</div>
		</div>
		<%@modes.each do |m|%>
	    <div class="row">
	        <div class="col-xs-12 col-sm-4 course-mode">
	            <%=m["name"]%>
	        </div>
	        <div class="col-xs-12 col-sm-8 course-availability">
                <div class="col-xs-4 col-sm-4 course-description first-month">
                	<%courses_available = available_courses_for_month(@courses,@selected_month.to_i,m["name"]).to_s%>
                	<%if courses_available.to_i > 0%>
                    	<%=link_to courses_available.to_s+" Cursos", :controller => :site, :action => :selected_courses_list, :params => {:mode => m["name"], :month => @selected_month.to_i}%>
                    <%else%>
                    	<i class="fa fa-times fa-lg"></i>
                    <%end%>
                </div>
                <div class="col-xs-4 col-sm-4 course-description second-month">
                	<%courses_available = available_courses_for_month(@courses,@selected_month.to_i+1,m["name"]).to_s%>
                	<%if courses_available.to_i > 0%>
                    	<%=link_to courses_available.to_s+" Cursos", :controller => :site, :action => :selected_courses_list, :params => {:mode => m["name"], :month => @selected_month.to_i+1}%>
                    <%else%>
                    	<i class="fa fa-times fa-lg"></i>
                    <%end%>
                </div>
                <div class="col-xs-4 col-sm-4 course-description third-month">
                	<%courses_available = available_courses_for_month(@courses,@selected_month.to_i+2,m["name"]).to_s%>
                	<%if courses_available.to_i > 0%>
                    	<%=link_to courses_available.to_s+" Cursos", :controller => :site, :action => :selected_courses_list, :params => {:mode => m["name"], :month => @selected_month.to_i+2}%>
                    <%else%>
                    	<i class="fa fa-times fa-lg"></i>
                    <%end%>
                </div>
	        </div>
	    </div>
	    <%end%>
	    <div class="row quotation-advise">
	    	<h2>Cotiza con Nosotros!</h2>
	    	<div class="col-sm-10 col-sm-offset-1">
	    		<p>Llama al 800-800-Longbourn y cotiza con nosotros!</p>
	    		<p>Tambien puedes visitarnos en nuestra sede en Coronel Pereira ASDfasdasd #1231451</p>
	    	</div>
	    </div>
	 </div>
</div>